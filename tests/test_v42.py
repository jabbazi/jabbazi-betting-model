import unittest\nfrom datetime import datetime, timezone\n\nfrom jabbazi_v42 import (\n    ModelStage, ValidationMetrics, evaluate_promotion,\n    validate_complementary_pair, break_even_probability,\n)\n\n\nclass V42Tests(unittest.TestCase):\n    def test_break_even_minus_110(self):\n        self.assertAlmostEqual(break_even_probability(-110), 110/210, places=6)\n\n    def test_complementary_pair_kill_switch(self):\n        self.assertTrue(validate_complementary_pair([0.52, 0.48]))\n        self.assertFalse(validate_complementary_pair([0.3215, 0.1785]))\n\n    def test_strong_model_can_reach_production(self):\n        m = ValidationMetrics(\n            sample_size=1600, brier=0.22, log_loss=0.64,\n            calibration_slope=1.01, calibration_intercept=0.01,\n            mean_clv_prob_points=0.7, market_baseline_brier=0.225,\n        )\n        self.assertEqual(evaluate_promotion(m).stage, ModelStage.PRODUCTION_APPROVED)\n\n    def test_weak_model_cannot_reach_production(self):\n        m = ValidationMetrics(\n            sample_size=2000, brier=0.27, log_loss=0.75,\n            calibration_slope=1.5, calibration_intercept=0.2,\n            mean_clv_prob_points=-1.0, market_baseline_brier=0.24,\n        )\n        self.assertNotEqual(evaluate_promotion(m).stage, ModelStage.PRODUCTION_APPROVED)\n\n\nif __name__ == '__main__':\n    unittest.main()